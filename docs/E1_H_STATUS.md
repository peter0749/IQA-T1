# E1 H：保留集效益（E1c）

2026-09-07。H32 已依凍結協定跑完。**判定：inconclusive。** 未訓練、未改 prompt、未補抽樣。

## 範圍

- 凍結 H32 與 split 內循環 donor；G0 同一 harness；D16 未載入推論。
- 真實 evidence vs 首次同類錯配。無自然呼叫者不強迫叫工具。
- 主要品質量：L5 的 mean(|error_true| − |error_swapped|)，僅雙有效自然呼叫對；負值表示真實 evidence 較準。
- 提升訊號（預先規定）：CI 上界 < 0、點估計改善 ≥ 0.05、真實 coverage ≥ 錯配。區間跨 0 ⇒ inconclusive，不宣稱等效或「沒使用 evidence」。

## 結果

| Checkpoint | 無呼叫 | 適用／雙有效／prefix | 真實 coverage | 主要品質差 mean [95% CI] | 分數絕對變化 |
| --- | ---: | --- | ---: | --- | --- |
| base | 32/32 | 0 | 0 | 不適用 | — |
| L2 | 20/32 | 12／12／12 | 32/32 | +0.042 [−0.025, 0.150] | 0.058 [0.00, 0.158] |
| **L5** | **0/32** | **32／32／32** | **32/32** | **−0.044 [−0.122, 0.025]** | 0.157 [0.075, 0.256] |

L5 全數有分數、格式 32/32、prefix 32/32。點估計真實分支略準 0.044 分，不到 0.05 門檻；CI 上界 0.025 > 0。**不通過實用提升訊號。**

無效輸出敏感性：L5 無無效分數，填 0 或 4 與主結果相同。L2 適用 12 張也都有分數。

L5 真實 MAE 0.322、錯配 0.366（成對差即上表 −0.044）。32 張裡 18 張換 evidence 分數不變。L5 第一工具 32/32 仍是 `GradientMagnitudeHistogram`。

L2 真實 MAE 0.333（32 張都有分數，含未呼叫）；適用 12 張的品質差區間跨 0，點估計錯配略準。base 32/32 `eos`、無分數，不是無工具基準。

L5 相對 L2 真實 rollout 的成對 |error| 差約 −0.011，只作次要描述。

## 判定

**inconclusive。** 證據可以改 L5 分數（絕對變化區間不含 0），但保留集上看不出穩定變準。不能把 D 上的工具套路當成品質失敗，也不能把 H 的負點估計當成效益。

對應協定決策第 6 條：保留 inconclusive，不自動加訓練、不補抽、不改 H。是否值得加大樣本由成本／效果另決，本次不執行。

## 產物與耗時

- Runner：`scratch/e1/h.py`
- 產物：`scratch/e1/h/run1/`（manifest、true/swap traces、analysis.json）
- 約 28 分鐘（base 571s、L2 374s、L5 738s）。G0+D+H 皆在 4 小時模型上限內。
