# E1 D：診斷集機制對照（E1a / E1b）

2026-09-07。D16 機制對照已完成。**不是保留集品質結論。未查看 H32。**

## 範圍

- 凍結 D16 與循環 donor；共同短 prompt；G0 同一 harness。
- 48 次真實 rollout（16×3）+ 24 次首次 evidence 錯配 continuation（L2 8、L5 16；base 無自然呼叫故 0）。
- 錯配：同工具、donor 圖，resize 到 receiver evidence 尺寸。後續工具回填 receiver 真圖。無呼叫者不強迫叫工具。
- 全數 `post_evidence_input_ids` 與真實分支相符（L2 8/8、L5 16/16）。

## E1a：原圖是否改變第一個工具

| Checkpoint | 無呼叫 | 第一工具 | 循環配對第一工具改變率（全體 / 雙方都有呼叫） |
| --- | ---: | --- | --- |
| base | 16/16 | — | 0 / 不適用 |
| L2 | 8/16 | 6×Histogram、1×Map、1×Colorfulness、8×無 | 0.625 / 0.50（僅 4 對雙方都有呼叫） |
| L5 | 0/16 | **16/16 GradientMagnitudeHistogram** | **0 / 0** |

L5 的第一工具對原圖輪換完全不變，符合「固定工具套路」假設在第一呼叫上的預期。L2 樣本小、無呼叫多，不能單獨判成已學會依圖選工具。base 在此 prompt 下沒有工具行為，不是無工具基準。

L5 真實序列 15/16 為 Histogram → Colorfulness → NoiseResidual；1/16 只叫 Histogram。這是描述，不是標註對錯。

## E1b：第一次工具結果換掉之後

主要機制量：自然呼叫子集上 `|score_true − score_swap|`，image-level bootstrap 10,000、seed 20260907。

| Checkpoint | 適用 / 雙有效 / prefix | 分數絕對變化 mean [95% CI] | 後續工具序列改變 | 真實相對錯配的 \|error\| 差 mean [95% CI] |
| --- | --- | --- | ---: | --- |
| base | 0 / — / — | 不適用 | — | — |
| L2 | 8 / 8 / 8 | 0.0125 [0.00, 0.0375] | 1/8 | 0.0125 [0.00, 0.0375] |
| L5 | 16 / 15 / 16 | **0.161 [0.027, 0.369]** | 2/16 | 0.021 [−0.037, 0.105] |

L5 的分數變化區間不含 0：第一次 evidence 有時會改分數。15 個雙有效對裡 9 個變化為 0；1.40 分那一筆（`4979581696`）權重很大。區間不含 0 不表示多數圖都敏感。

真實相對錯配的誤差差區間跨 0：在 D16 上看不出真實 evidence 比較準。這本來就不是保留集品質檢定，不能拿來宣稱 H2/H3/H4。

L2 分數幾乎不動（8 張裡 7 張變化 0），比較接近「回傳證據影響很小」，但適用子集只有 8 張。

## 明確不是結論

- 未估計 H32 效益；未訓練；未改 prompt。
- L5 第一工具塌縮 ≠ 工具選錯。標註清單不是唯一正解。
- 分數被換掉 ≠ 證據有用。D16 誤差差跨 0。
- `6937691669` 在 L5 真實與錯配都沒解析到分數，不進入雙有效子集。

## 產物與耗時

- Runner：`scratch/e1/d.py`（G0 harness）
- 產物：`scratch/e1/d/run1/`（manifest、true/swap traces、analysis.json）
- 約 15.4 分鐘（base 290s、L2 235s、L5 390s）。依此粗估 H32×2×3 仍低於 4 小時上限；**這不是授權跑 H**。

## 對假設的縮小（僅 D16）

1. L5 第一工具高度集中且不隨原圖改變 → 後續若看品質，應把「通用工具套路」當候選，不因 overlap 低判失敗。
2. L5 首次 evidence 可以改分數，但 D16 上看不到穩定變準 → 敏感性與效益必須分開；效益只在 H 上判定。
3. L2 半數不叫工具，叫了以後 evidence 幾乎不改分數。
4. base 無工具路徑，不能當公平無工具對照。

下一步依協定是 **H 保留集（E1c）**，需另一次 Outcome。
